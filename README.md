# Watch_party_backend

This repo serves as the backend for the project watch party.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

The server runs using Node.js and uses ES modules. Dependencies include Fastify, @fastify/cors, and Socket.io.
